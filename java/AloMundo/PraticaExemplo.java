package AloMundo;

class PraticaPrimeira {
  public static void main(String vetor[]) {
    int n1, n2, soma;
    n1 = Integer.parseInt(vetor[0]);
    n2 = Integer.parseInt(vetor[1]);
    soma = n1 + n2;
    System.out.println(n1 + "+" + n2 + "=" + soma);
    System.exit(0);
  }
}
