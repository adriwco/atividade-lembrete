package AloMundo;

class java{
  // módulo principal com a entrada pela linha de comando  class java {}

  // integer.parseInt é uma função que recebe String e conveter para int
  // ex: o texto "41" (dois caracter) entra na função, converte em números e amarzena na váriavel n1 (int)

  // public deixa o procedimento público
  // static é porque nunca vai mudar
  // entre os parenteses tem os parametros que é um vetor chamado "entrada" do
  // tipo string (conjunto de caracter), quando é somente um é char
  // declaração de variáveis (tipos primitivos):

  // public static void main = metodo principal ; é por onde executa o programa
  // tudo fora do main são partes do programa, serão executados apenas se for chamado dentro do main
  // void e main é um procedimento com parametro que recebe váriavel/vetor args do tipo String 

  // ! o charAt convter para caracter "!"
  // o (double)NumInt  ou (int)NumReal é conveter 
  // div = (int)n1 / (int)n2 da o resto;  div = (double)n1 / (double)n2 da o resultado;
  // O Math é uma grande biblioteca

}